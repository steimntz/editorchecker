language: node

services:
  - docker

script:
  - docker build -t steimntz/editorchecker

deploy:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push steimntz/editorchecker
